---
concern: a remeasure rig's @new probe comment escaped to the LIVE backend and spawned a real worker — and the live dispatch left NO persisted thread. Evidence: session 5bd86fb7 (real-repo worktree event-detail-5bd8, live :8787 board) was dispatched by an @new mention on thread 'eval-event-detail-eval-comments' whose body carried nonce 1783362274654 — the exact probe text of session a936b772's eval-comments re-measure (its reading at 72186b22, ts 2026-07-06T18:28Z, note admits dispatch flash="" — it filed pass without observing the echo, which landed on a different backend's page). Meanwhile the live trunk store holds NO eval-event-detail-eval-comments file, no issue:/remark: commit for it anywhere (git log --all), and the board overlay carries no entry.thread — the live backend fired a mention dispatch off a thread write that never landed (the spawned worker's thread ref dangled: /api/issues/<id> 404). Two smells: (1) measurement rigs must pin API_URL/SPEXCODE_API_URL explicitly — the vite proxy's :8787 default plus the env a dispatched shell inherits makes cross-backend probe leakage a one-mistake accident; (2) the remark write path's dispatch should be atomic with thread persistence — never dispatch off a write that didn't land. A clean re-measure of eval-comments (echo observed: '@ new→3c2cc69c…') is filed at 0566e64f on node/event-detail-5bd8. [[remark-substrate]] [[mentions]]
by: 5bd86fb7-24a8-4ce0-843d-195c951c9bbe
status: open
nodes: remark-substrate, mentions
created: 2026-07-06T18:56:47.172Z
---

(no detail given — a remeasure rig's @new probe comment escaped to the LIVE backend and spawned a real worker — and the live dispatch left NO persisted thread. Evidence: session 5bd86fb7 (real-repo worktree event-detail-5bd8, live :8787 board) was dispatched by an @new mention on thread 'eval-event-detail-eval-comments' whose body carried nonce 1783362274654 — the exact probe text of session a936b772's eval-comments re-measure (its reading at 72186b22, ts 2026-07-06T18:28Z, note admits dispatch flash="" — it filed pass without observing the echo, which landed on a different backend's page). Meanwhile the live trunk store holds NO eval-event-detail-eval-comments file, no issue:/remark: commit for it anywhere (git log --all), and the board overlay carries no entry.thread — the live backend fired a mention dispatch off a thread write that never landed (the spawned worker's thread ref dangled: /api/issues/<id> 404). Two smells: (1) measurement rigs must pin API_URL/SPEXCODE_API_URL explicitly — the vite proxy's :8787 default plus the env a dispatched shell inherits makes cross-backend probe leakage a one-mistake accident; (2) the remark write path's dispatch should be atomic with thread persistence — never dispatch off a write that didn't land. A clean re-measure of eval-comments (echo observed: '@ new→3c2cc69c…') is filed at 0566e64f on node/event-detail-5bd8. [[remark-substrate]] [[mentions]])
