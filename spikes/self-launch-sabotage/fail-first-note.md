# Fail-first note

`fail-first.log` is the immutable first invocation of this lane's gate against integration HEAD `dd228c4eb`.
Its SHA-256 is `1fd9e4326acd348e4d0fbd13ce5744368674abba58395bef5dc0a32c25cff909`; `fail-first.exit` records exit `2`.

The run proves that the fixture was created, every printed project/home/temp/database/pack path resolved below it,
and a failed clean-build prerequisite became five explicit `NOT-MEASURED` counts rather than zeros. The retained
`evidence/fail-first/build.log` identifies the apparatus failure: the gate omitted optional dependencies even though
the root package's optional Hono packages are required to type-check `spec-cli`.

It proves nothing about the seven product attacks, static reachability, or runtime file access because execution
stopped before those surfaces. The gate was corrected by installing the complete lockfile dependency set. Neither
the original log nor its exit record is regenerated by later runs.
