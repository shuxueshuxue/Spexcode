#!/usr/bin/env node
// @@@ root release launcher ([[release-launcher]]) - the metapackage owns one command and delegates all
// product behavior to the independently compiled CLI package.
import '@spexcode/spec-cli/bin/spex.mjs'
