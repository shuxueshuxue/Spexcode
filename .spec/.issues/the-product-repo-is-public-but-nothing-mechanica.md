---
concern: the product repo is PUBLIC, but nothing mechanical stops a lane from committing host-identifying evidence into it. Two fleet-census/cleanup ledger nodes plus three reports/*.md lived on public main for days carrying session/thread ids, PIDs, a live relay endpoint (64.83.11.237:18080), intranet + tailnet addresses, home paths, an ssh access recipe, and another project's internal bug forensics — deleted at 62de2930, but public git history still serves every blob by sha. spec lint had 0 errors the whole time: a node governing reports/foo-2026-07-27.md is structurally indistinguishable from one governing src/foo.ts. Worth considering: a coverage/altitude signal for spec nodes whose code: target is a dated one-shot document rather than a maintained source file, and/or a doctor check that flags host identities (absolute home paths, RFC1918/CGNAT addresses, ssh recipes, PIDs) in tracked spec bodies and governed docs. The prose boundary now lives in the host-resource-budget body, which is the only thing enforcing it.
by: 1f3f3627-faca-489f-96aa-ff8eb6f3c77b
status: open
nodes: host-resource-budget
created: 2026-07-29T08:23:51.258Z
---

(no detail given — the product repo is PUBLIC, but nothing mechanical stops a lane from committing host-identifying evidence into it. Two fleet-census/cleanup ledger nodes plus three reports/*.md lived on public main for days carrying session/thread ids, PIDs, a live relay endpoint (64.83.11.237:18080), intranet + tailnet addresses, home paths, an ssh access recipe, and another project's internal bug forensics — deleted at 62de2930, but public git history still serves every blob by sha. spec lint had 0 errors the whole time: a node governing reports/foo-2026-07-27.md is structurally indistinguishable from one governing src/foo.ts. Worth considering: a coverage/altitude signal for spec nodes whose code: target is a dated one-shot document rather than a maintained source file, and/or a doctor check that flags host identities (absolute home paths, RFC1918/CGNAT addresses, ssh recipes, PIDs) in tracked spec bodies and governed docs. The prose boundary now lives in the host-resource-budget body, which is the only thing enforcing it.)
