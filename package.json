{
  "name": "spexcode-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "SpexCode dev monorepo (private, unpublished). The published npm package is `spexcode`, built from spec-cli — see .spec/spexcode/spec-cli/packaging.",
  "scripts": {
    "api": "npm --prefix spec-cli run serve",
    "web": "npm --prefix spec-dashboard run dev",
    "lint": "npm --prefix spec-cli run -s lint",
    "seed": "bash scripts/seed-spec-history.sh",
    "hooks": "bash scripts/install-hooks.sh"
  }
}
