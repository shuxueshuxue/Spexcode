
### busy-timeout-after-version-probe
    claim   : busy_timeout must be the first statement on a connection
    verdict : GATED
      - repeated cold opens by eight processes never lose the first-open race
      - eight processes racing initialize on one address converge without duplication
      - busy_timeout must be the connection's first statement, or the version probe has no budget

### producer-minted-message-id
    claim   : message_id is protocol-generated and is not part of the payload hash
    verdict : GATED
      - message_id is protocol-generated; a producer-supplied one is refused
      - payload_hash preimage is reproducible from the written specification alone
      - header order on input never changes the hash; header keys are ASCII-only
      - exact idempotent replay returns the first row; changed bytes conflict
      - an honest retry must not be punished for minting a fresh message id

### whole-schema-checksum
    claim   : each migration checksums only its own bytes
    verdict : GATED
      - each migration checksums only its own bytes

### unvalidated-session-id
    claim   : session ids are validated in memory; STRICT is not input validation
    verdict : GATED
      - every SQL feature the floor is derived from actually works at the floor
      - session ids: accepted and rejected character sets
      - the address space is flat and global within one database path
      - session ids: a non-string id is rejected in memory, never coerced by SQLite affinity

### unpinned-indexes
    claim   : the hot statements pin their index instead of trusting planner statistics
    verdict : GATED
      - every SQL feature the floor is derived from actually works at the floor
      - every declared index is the one the planner actually uses

### string-body-accepted
    claim   : the body is opaque bytes; the protocol never picks an encoding for the producer
    verdict : GATED
      - body must be explicit bytes; a string is not an encoding the protocol guesses

### recursive-parent-mkdir
    claim   : a misspelled path must not grow a plausible database tree
    verdict : GATED
      - open path: a missing parent directory fails loudly and creates nothing

### converts-wal-instead-of-refusing
    claim   : a database left in WAL is refused; v1 has no runtime dual path and never converts
    verdict : GATED
      - a database left in WAL is refused, not converted

### at-least-once-redelivery
    claim   : dequeue commits at-most-once delivery; a dead handler never gets the message re-handed
    verdict : GATED
      - concurrent consumers of one queue split it without a double dequeue
      - a handler that dies after dequeue never makes the message reappear
      - SIGKILL after commit keeps the message and never requeues it
      - retirement keeps the tombstone and the whole history readable
      - FIFO order, and null means exactly one thing

### locality-detection-in-protocol-core
    claim   : storage locality is the adopter resolver's fail-closed precondition, not protocol core's job
    verdict : GATED
      - protocol core neither performs nor claims a storage-locality determination

gated 10/10   ungated 0   not measured 0
